import { TimeDecreasePipe } from './time-decrease.pipe';

describe('TimeDecreasePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeDecreasePipe();
    expect(pipe).toBeTruthy();
  });
});
