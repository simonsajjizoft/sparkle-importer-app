import { TextHighlighterPipe } from './text-highlighter.pipe';

describe('TextHighlighterPipe', () => {
  it('create an instance', () => {
    const pipe = new TextHighlighterPipe();
    expect(pipe).toBeTruthy();
  });
});
