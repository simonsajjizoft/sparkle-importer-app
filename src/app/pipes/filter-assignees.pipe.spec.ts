import { FilterAssigneesPipe } from './filter-assignees.pipe';

describe('FilterAssigneesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssigneesPipe();
    expect(pipe).toBeTruthy();
  });
});
